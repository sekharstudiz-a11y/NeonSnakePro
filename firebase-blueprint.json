{
  "entities": {
    "Score": {
      "title": "Score",
      "description": "A high score entry for the leaderboard.",
      "type": "object",
      "properties": {
        "displayName": {
          "type": "string",
          "description": "The name of the player."
        },
        "score": {
          "type": "integer",
          "description": "The player's raw score."
        },
        "weightedScore": {
          "type": "integer",
          "description": "The player's score multiplied by difficulty."
        },
        "difficulty": {
          "type": "string",
          "enum": ["EASY", "MEDIUM", "HARD", "INSANE"],
          "description": "The difficulty level."
        },
        "timestamp": {
          "type": "string",
          "format": "date-time",
          "description": "When the score was achieved."
        },
        "uid": {
          "type": "string",
          "description": "The unique ID of the player."
        }
      },
      "required": ["displayName", "score", "weightedScore", "difficulty", "timestamp", "uid"]
    }
  },
  "firestore": {
    "/leaderboard/{scoreId}": {
      "schema": "Score",
      "description": "Global leaderboard entries."
    }
  }
}
